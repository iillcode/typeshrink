{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Element Click Browser",
      "type": "extensionHost",
      "request": "launch",
      "args": ["--extensionDevelopmentPath=${workspaceFolder}"],
      "outFiles": ["${workspaceFolder}/out/**/*.js"]
    }
  ]
}
